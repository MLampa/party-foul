<div class="column row-height">
  Welcome, <%= current_user.username %>!
  <li><%= link_to "Edit Profile", edit_user_registration_path %></li>
</div>

<div>
  Your Events:<br>
  <% @events.each do |event| %>
    <%= event.event_name %>
  <% end %>
</div>
